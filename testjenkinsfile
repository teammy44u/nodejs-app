pipeline{
    agent any
    stages{
        stage('dependency install'){
            agent { docker { image 'nodejs'}}
            steps{
            //    sh 'sudo chown -R 995:993 "/.npn"'
                sh 'npm install'
            }
        }
    }
}